{
  "name": "business-escalate-cloud",
  "version": "1.0.0",
  "description": "Cloud backend for Business Escalate (Postgres + JWT + server-side AI).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "web-push": "^3.6.7"
  }
}
